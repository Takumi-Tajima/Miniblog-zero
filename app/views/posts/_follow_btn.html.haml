- if user_signed_in? && current_user != post.user
  - if current_user.following?(post.user)
    = button_to 'フォロー解除', relationship_path(post.user_id), method: :delete, class: 'btn btn-primary'
  - else
    = button_to 'フォロー', relationships_path(user_id: post.user_id), method: :post, class: 'btn btn-danger'


