- if user_signed_in? && post.liked_by?(current_user)
  = button_to post_likes_path(post.id), class: 'btn', method: :delete do
    %i.fas.fa-regular.fa-heart.likes_btn.text-danger
- else
  = button_to post_likes_path(post.id), class: 'btn' do
    %i.fas.fa-solid.fa-heart.unlikes_btn
