%ul.list-group.list-group-flush
  - @posts.each do |post|
    %li.list-group-item.p-2
      .fs-6
        = post.user.name
      .d-flex.justify-content-between
        = link_to post.content, post_path(post), class: 'text-decoration-none text-body'
        = render 'posts/follow_btn', post: post
      .text-sm.text-end.mt-2.text-secondary
        = l post.created_at
        = button_to 'いいね取り消し', post_likes_path(post.id), method: :delete
        = button_to 'いいね', post_likes_path(post.id)
